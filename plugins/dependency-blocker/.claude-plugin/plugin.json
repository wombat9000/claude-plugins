{
  "name": "dependency-blocker",
  "description": "Prevents Claude from accessing dependency directories to save tokens",
  "version": "1.0.0",
  "hooks": {
    "bashValidate": "${CLAUDE_PLUGIN_ROOT}/scripts/bash-validate.sh",
    "readValidate": "${CLAUDE_PLUGIN_ROOT}/scripts/read-validate.sh"
  }
}

{
  "name": "dependency-blocker",
  "description": "Prevents Claude from accessing dependency directories to save tokens",
  "version": "1.0.0",
  "hooks": {
    "bashValidate": "${CLAUDE_PLUGIN_ROOT}/hooks/bash-validate.sh",
    "readValidate": "${CLAUDE_PLUGIN_ROOT}/hooks/read-validate.sh"
  }
}
